import { makeStyles } from "@material-ui/core/styles";

const globalUseStyles = makeStyles((theme) => ({
  primaryColor: {
    backgroundColor: "#5c049b",
  },
  secondaryColor: {
    backgroundColor: "#eaf5e7",
  },
  terciaryColor: {
    backgroundColor: "#0eb1d4",
  },
}));

export default globalUseStyles;
